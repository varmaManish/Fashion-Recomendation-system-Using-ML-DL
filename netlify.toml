[build]
  command = "pip install -r requirements.txt"
  publish = "frontend"

[functions]
  directory = "netlify/functions"  # Add this line
  included_files = ["backend/**"]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api"
  status = 200
